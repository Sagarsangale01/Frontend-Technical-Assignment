# Frontend-Technical-Assignment
A frontend assignment to build a React based store locator application with an interactive map. The app displays store locations and service coverage areas, allows filtering stores by type, syncing a store list with map interactions, and adding new stores dynamically, focusing on clean UI, state management, and map integration.
